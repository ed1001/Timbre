<% if max_dimension == 'width' %>
  <%= cl_image_tag(pic.photo.metadata['public_id'],
        width: 500, crop: :fill, class: "d-block", id: "target") %>
<% else %>
  <%= cl_image_tag(pic.photo.metadata['public_id'],
        height: 500, crop: :fill, class: "d-block", id: "target") %>
<% end %>
